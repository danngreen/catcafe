node_modules/
.DS_Store
*.log
/shots/
# The valleys the LAN server keeps between sessions.
valley.json
saves/

# Copies the content editor takes before it writes. Local safety net, not history.
content-backups/
